# OpenCode — Architecture and Development Constraints

> Generated by: architect (init-scan)
> Last scan: 2026-06-14
> Scan tool: CodeGraph available (2106 files indexed, 31876 nodes, 73399 edges)
> Confidence: confirmed 78% / inferred 17% / assumed 5%

---

## 1. System Architecture

- **Architecture pattern**: Feature-sliced monorepo with layered internals in the main application package [CONFIRMED]
- **Technical stack**: TypeScript · Bun 1.3 · Effect 4.0-beta · SolidJS 1.9 · Drizzle ORM 1.0-rc · SQLite · Hono · Electron 41 · Turborepo 2.8 [CONFIRMED]
- **Deployment model**: Monorepo with multi-target outputs (CLI binary, web app, desktop app, cloud functions, VS Code extension) [CONFIRMED]
- **Module count**: 22 packages under `packages/` + 1 SDK under `sdks/` [CONFIRMED]
- **Default branch**: `stable` (not `main`) [CONFIRMED]
- **Runtime**: Bun-first with Node.js fallback via conditional `imports` in `packages/opencode/package.json` (`#db`, `#pty`, `#hono`) [CONFIRMED]

---

## 2. Module Inventory

| Module | Path | Responsibility | Depends On | Confidence |
|---|---|---|---|---|
| **opencode** | `packages/opencode` | Main CLI/TUI application + HTTP server + session orchestrator + tool system + DAG engine | core, llm, plugin, sdk, ui | [CONFIRMED] |
| **core** | `packages/core` | Shared schemas, model definitions, provider configs, utilities, session types | (none) | [CONFIRMED] |
| **llm** | `packages/llm` | Effect Schema-first LLM abstraction layer: protocols, routes, providers, transports | (none — Effect only) | [CONFIRMED] |
| **app** | `packages/app` | SolidJS web frontend application (Vite + TailwindCSS) | core, sdk, ui | [CONFIRMED] |
| **ui** | `packages/ui` | Shared SolidJS UI component library, themes, icons, i18n | core, sdk | [CONFIRMED] |
| **desktop** | `packages/desktop` | Electron desktop application wrapper (electron-vite) | app, ui | [CONFIRMED] |
| **plugin** | `packages/plugin` | Public plugin API: tool definitions, TUI hooks, SDK integration | sdk | [CONFIRMED] |
| **sdk** | `packages/sdk/js` | Generated JavaScript SDK client from OpenAPI spec | (none) | [CONFIRMED] |
| **effect-drizzle-sqlite** | `packages/effect-drizzle-sqlite` | Vendored Drizzle + Effect SQLite adapter (generic, no domain code) | (drizzle-orm, effect) | [CONFIRMED] |
| **enterprise** | `packages/enterprise` | Enterprise features (SolidStart, Cloudflare Workers target) | core, ui | [CONFIRMED] |
| **console** | `packages/console` | Admin console: sub-monorepo with app, core, function, mail, resource | core (some sub-packages) | [CONFIRMED] |
| **function** | `packages/function` | SST cloud functions (serverless) | core | [CONFIRMED] |
| **slack** | `packages/slack` | Slack integration | (workspace) | [CONFIRMED] |
| **script** | `packages/script` | Build, publish, and release automation scripts | (none) | [CONFIRMED] |
| **http-recorder** | `packages/http-recorder` | HTTP request/response recording for replay-based testing | (none) | [CONFIRMED] |
| **storybook** | `packages/storybook` | Storybook for UI component development | ui | [CONFIRMED] |
| **web** | `packages/web` | Marketing website (Astro) | (none) | [CONFIRMED] |
| **containers** | `packages/containers` | Docker container definitions (base, bun-node, publish, rust) | (none) | [CONFIRMED] |
| **identity** | `packages/identity` | Brand assets: logos, icons (SVG/PNG) | (none) | [CONFIRMED] |
| **extensions** | `packages/extensions` | IDE extensions (Zed) | (none) | [CONFIRMED] |
| **vscode** | `sdks/vscode` | VS Code extension | (standalone) | [CONFIRMED] |

### Dependency Direction (feature-sliced model)

```
desktop ──→ app ──→ ui ──→ core
                        ↘  sdk
opencode ──→ core, llm, plugin, sdk, ui
enterprise ──→ core, ui
console/* ──→ core (selective)
function ──→ core
```

- Upper-layer packages (desktop, app, opencode) depend on lower-layer packages (core, ui, sdk, llm). [CONFIRMED]
- Lateral dependencies between application packages (app ↔ opencode) are forbidden. [CONFIRMED]
- `core` and `llm` are leaf packages with no workspace dependencies. [CONFIRMED]
- `effect-drizzle-sqlite` is infrastructure-only; no domain code. [CONFIRMED]

### Dependency Graph — `packages/opencode` Internal Architecture

```
cli/ (entry point, yargs commands)
  └→ session/ (orchestrator: LLM calls + tools + message management)
       ├→ tool/ (tool registry + individual tool implementations)
       ├→ agent/ (agent definitions + prompt generation)
       ├→ llm.ts + llm/ (LLM adapter: AI SDK path + native @opencode-ai/llm path)
       ├→ permission/ (permission rules + rulesets)
       ├→ hook/ (session hooks + agent tool builder)
       ├→ skill/ (skill discovery + prompt injection)
       └→ prompt/ (system prompt assembly)
  └→ server/ (Hono HTTP + WebSocket server, serves API for web UI)
  └→ config/ (multi-layer config: global → project → local)
  └→ storage/ (Drizzle ORM + SQLite, dual Bun/Node runtime)
  └→ dag/ (DAG workflow engine: execution-core → session → bridge/query)
  └→ mcp/ (Model Context Protocol integration)
  └→ plugin/ (plugin loading + provider auth plugins)
  └→ provider/ (model provider registry + transforms)
  └→ bus/ (intra-app event pub/sub)
  └→ git/ (git operations)
  └→ pty/ (pseudo-terminal, dual Bun/Node)
```

---

## 3. Interface Boundary

### Cross-Module Public API

| Source Module | Interface | Consumer Module(s) | Evidence | Confidence |
|---|---|---|---|---|
| core | `@opencode-ai/core/*` (wildcard exports) | opencode, app, ui, enterprise, function | `packages/core/package.json` exports field | [CONFIRMED] |
| llm | `@opencode-ai/llm`, `/route`, `/provider`, `/providers/*`, `/protocols/*` | opencode | `packages/llm/package.json` exports field | [CONFIRMED] |
| sdk | Generated JS client types | opencode, app, ui, plugin | `packages/sdk/js/package.json` | [CONFIRMED] |
| ui | `@opencode-ai/ui/*` (components, hooks, context, theme, styles) | app, desktop, enterprise | `packages/ui/package.json` exports field | [CONFIRMED] |
| plugin | `@opencode-ai/plugin`, `/tool`, `/tui` | opencode | `packages/plugin/package.json` exports field | [CONFIRMED] |
| opencode | `opencode/*` (wildcard exports for test/integration) | (internal only) | `packages/opencode/package.json` exports field | [CONFIRMED] |
| effect-drizzle-sqlite | `.`, `/effect-sqlite`, `/sqlite-core/effect` | opencode | `packages/effect-drizzle-sqlite/package.json` exports field | [CONFIRMED] |

### Iron Interfaces (must not bypass)

- **llm ↔ session boundary**: `packages/llm` must stay independent of session concerns. Session auth, permissions, plugins, telemetry, and runtime selection belong in `packages/opencode/src/session/llm.ts` and its local adapters. [CONFIRMED — `packages/llm/AGENTS.md`]
- **effect-drizzle-sqlite domain neutrality**: No opencode-specific tables, paths, migrations, or domain storage APIs. [CONFIRMED — `packages/effect-drizzle-sqlite/AGENTS.md`]
- **Desktop IPC boundary**: Renderer process calls only `window.api` from `src/preload`; main process registers IPC handlers in `src/main/ipc.ts`. [CONFIRMED — `packages/desktop/AGENTS.md`]
- **DAG state machine**: All state changes must go through state machine API; no direct status variable mutation. Terminal states (`completed`, `failed`, `cancelled`) are immutable. [CONFIRMED — `packages/opencode/src/dag/AGENTS.md`]

---

## 4. Design Pattern

- **Unified pattern**: Feature-sliced monorepo at top level; Effect-based service layer with DI (Context/Tag/Layer) within `packages/opencode`; provider-route-protocol decomposition in `packages/llm` [CONFIRMED]
- **Effect service convention**: Services in `packages/opencode` use `Effect.Context` for DI, `Effect.Layer` for provision, `Effect.gen` for composition. Service modules export a `Service` tag and a `Layer`. [CONFIRMED — `packages/opencode/src/session/session.ts`, `packages/opencode/src/tool/registry.ts`]
- **Dual-runtime pattern**: Conditional `imports` in `packages/opencode/package.json` (`#db`, `#pty`, `#hono`) select Bun vs Node implementations at runtime. [CONFIRMED]
- **LLM route decomposition** (in `packages/llm`): Four-axis design — Protocol (API contract) × Endpoint (URL) × Auth (credentials) × Framing (bytes→frames). Provider facades compose these via `Route.make(...)`. [CONFIRMED — `packages/llm/AGENTS.md`]
- **Forbidden patterns**: No circular workspace dependencies; no cross-layer bypass in DAG engine; no opencode domain code in `effect-drizzle-sqlite`; no session concerns in `llm`. [CONFIRMED]

---

## 5. Responsibility Contract

### State Ownership

| State Category | Owner Module | Scope | Confidence |
|---|---|---|---|
| Session state (messages, LLM context) | `packages/opencode/src/session/` | Request-scoped (per session) | [CONFIRMED] |
| Global config | `packages/opencode/src/config/` | Global → project → local layers | [CONFIRMED] |
| Database (SQLite) | `packages/opencode/src/storage/` | Persistent, project-scoped | [CONFIRMED] |
| Event bus | `packages/opencode/src/bus/` | Process-scoped (in-memory) | [CONFIRMED] |
| DAG workflow state | `packages/opencode/src/dag/persistence/` | Persistent (SQLite), workflow-scoped | [CONFIRMED] |
| Provider model registry | `packages/opencode/src/provider/` | Process-scoped | [CONFIRMED] |
| Plugin state | `packages/opencode/src/plugin/` | Process-scoped | [CONFIRMED] |

### Behavior Boundary

| Behavior | Module | Confidence |
|---|---|---|
| CLI argument parsing | `packages/opencode/src/cli/` (yargs) | [CONFIRMED] |
| LLM request orchestration | `packages/opencode/src/session/llm.ts` + `llm/` adapters | [CONFIRMED] |
| Tool registration + execution | `packages/opencode/src/tool/registry.ts` | [CONFIRMED] |
| Permission enforcement | `packages/opencode/src/permission/` | [CONFIRMED] |
| HTTP API serving | `packages/opencode/src/server/` (Hono) | [CONFIRMED] |
| Config loading + watching | `packages/opencode/src/config/` | [CONFIRMED] |
| DB schema + migrations | `packages/opencode/src/storage/` + `migration/` | [CONFIRMED] |
| Agent prompt generation | `packages/opencode/src/agent/` + `session/prompt/` | [CONFIRMED] |
| Skill discovery + injection | `packages/opencode/src/skill/` | [CONFIRMED] |
| DAG workflow scheduling | `packages/opencode/src/dag/session/` (WorkflowEngine) | [CONFIRMED] |
| MCP server integration | `packages/opencode/src/mcp/` | [CONFIRMED] |
| Git operations | `packages/opencode/src/git/` | [CONFIRMED] |
| PTY management | `packages/opencode/src/pty/` | [CONFIRMED] |

---

## 6. Development Conventions

| Command | Purpose | Confidence |
|---|---|---|
| `bun turbo typecheck` | Type check all packages (uses `tsgo`, not `tsc`) | [CONFIRMED] |
| `bun turbo test:ci` | Run all tests in CI mode | [CONFIRMED] |
| `cd packages/opencode && bun test` | Run opencode unit tests | [CONFIRMED] |
| `cd packages/opencode && bun test src/dag` | Run DAG engine tests | [CONFIRMED] |
| `oxlint` | Lint all source files | [CONFIRMED] |
| `bun run --cwd packages/opencode dev` | Run CLI in dev mode | [CONFIRMED] |
| `bun run --cwd packages/app dev` | Run web app in dev mode | [CONFIRMED] |
| `bun run --cwd packages/desktop dev` | Run desktop app in dev mode | [CONFIRMED] |
| `./packages/sdk/js/script/build.ts` | Regenerate JavaScript SDK | [CONFIRMED] |
| `bun run --cwd packages/opencode db` | Run Drizzle Kit (DB migrations) | [CONFIRMED] |

### Naming Conventions

- Source files: `kebab-case.ts` / `kebab-case.tsx` [CONFIRMED]
- Drizzle schema fields: `snake_case` (no string argument to column constructors) [CONFIRMED]
- Effect services: PascalCase `Service` tag + `Layer` export [CONFIRMED]
- Test files: co-located in `test/` directory mirroring `src/` structure, or in `__tests__/` within source [CONFIRMED]
- Tool files: `<tool_name>.ts` + `<tool_name>.txt` (text description) in `packages/opencode/src/tool/` [CONFIRMED]
- Config modules: self-export pattern `export * as ConfigXxx from "./xxx"` at top of file [CONFIRMED]

### Test Conventions

- Unit test location: `packages/<pkg>/test/` (mirrors `src/` structure) or `__tests__/` within source directories [CONFIRMED]
- Integration test location: `packages/app/e2e/` (Playwright for web app) [CONFIRMED]
- Naming pattern: `<module>.test.ts` [CONFIRMED]
- Framework: `bun:test` (describe/expect/test) [CONFIRMED]
- Effect test helper: `testEffect(...)` from `test/lib/effect.ts` for Effect-based tests [CONFIRMED]
- Recorded tests: `recordedTests({ prefix, requires })` in `packages/llm` for provider replay testing [CONFIRMED]
- Test fixture: `tmpdir()` from `test/fixture/fixture.ts` for temporary directories with auto-cleanup [CONFIRMED]

---

## 7. Iron Laws

| # | Constraint | Evidence | Confidence |
|---|---|---|---|
| 1 | Tests cannot run from repo root; must run from package dirs | `package.json` line 21: `"test": "echo 'do not run tests from root' && exit 1"` | [CONFIRMED] |
| 2 | Typecheck uses `tsgo`, never `tsc` directly | `packages/opencode/package.json` line 10: `"typecheck": "tsgo --noEmit"` | [CONFIRMED] |
| 3 | Default branch is `stable` | `.github/workflows/test.yml` line 6: `branches: [stable, dev]` | [CONFIRMED] |
| 4 | Default branch is **not** `master`. Local `main` ref may not exist; use `stable` or `origin/stable` for diffs. | repo config + CI branches | [CONFIRMED] |
| 5 | All services in `packages/opencode` use Effect DI (Context/Tag/Layer pattern) | `src/session/session.ts`, `src/tool/registry.ts`, `src/provider/provider.ts` | [CONFIRMED] |
| 6 | `packages/llm` must stay independent of session concerns | `packages/llm/AGENTS.md` §Architecture | [CONFIRMED] |
| 7 | `packages/effect-drizzle-sqlite` must stay domain-neutral | `packages/effect-drizzle-sqlite/AGENTS.md` | [CONFIRMED] |
| 8 | DAG engine: state machine cannot be bypassed; terminal states immutable | `packages/opencode/src/dag/AGENTS.md` §2 铁律 | [CONFIRMED] |
| 9 | Conventional commits: `type(scope): summary` | §Commits and PR Titles (human-curated) | [CONFIRMED] |
| 10 | Avoid `any` type; prefer Bun APIs; prefer `const` + early returns | §Style Guide (human-curated) | [CONFIRMED] |
| 11 | Drizzle schema fields use `snake_case` without string arguments | §Style Guide §Schema Definitions | [CONFIRMED] |
| 12 | Avoid mocks in tests; test actual implementation | §Testing (human-curated) | [CONFIRMED] |
| 13 | Desktop renderer must only call `window.api` from preload | `packages/desktop/AGENTS.md` | [CONFIRMED] |
| 14 | SDK regeneration: `./packages/sdk/js/script/build.ts` | Line 1 of existing AGENTS.md | [CONFIRMED] |
| 15 | Config modules follow self-export pattern: `export * as ConfigX from "./x"` | §Style Guide line 26 | [CONFIRMED] |

---

## 8. Quick Tips

- To regenerate the JavaScript SDK, run `./packages/sdk/js/script/build.ts`.
- ALWAYS USE PARALLEL TOOLS WHEN APPLICABLE.
- The default branch in this repo is `stable`.
- Local `main` ref may not exist; use `stable` or `origin/stable` for diffs.
- Prefer automation: execute requested actions without confirmation unless blocked by missing info or safety/irreversibility.

## Commits and PR Titles

Use conventional commit-style messages and PR titles: `type(scope): summary`.

Valid types are `feat`, `fix`, `docs`, `chore`, `refactor`, and `test`. Scopes are optional; use the affected package or area when helpful, e.g. `core`, `opencode`, `tui`, `app`, `desktop`, `sdk`, or `plugin`.

Examples: `fix(tui): simplify thinking toggle styling`, `docs: update contributing guide`, `chore(sdk): regenerate types`.

## Style Guide

### General Principles

- Keep things in one function unless composable or reusable
- Do not extract single-use helpers preemptively. Inline the logic at the call site unless the helper is reused, hides a genuinely complex boundary, or has a clear independent name that improves the caller.
- Avoid `try`/`catch` where possible
- Avoid using the `any` type
- Use Bun APIs when possible, like `Bun.file()`
- Rely on type inference when possible; avoid explicit type annotations or interfaces unless necessary for exports or clarity
- Prefer functional array methods (flatMap, filter, map) over for loops; use type guards on filter to maintain type inference downstream
- In `src/config`, follow the existing self-export pattern at the top of the file (for example `export * as ConfigAgent from "./agent"`) when adding a new config module.

Reduce total variable count by inlining when a value is only used once.

```ts
// Good
const journal = await Bun.file(path.join(dir, "journal.json")).json()

// Bad
const journalPath = path.join(dir, "journal.json")
const journal = await Bun.file(journalPath).json()
```

### Destructuring

Avoid unnecessary destructuring. Use dot notation to preserve context.

```ts
// Good
obj.a
obj.b

// Bad
const { a, b } = obj
```

### Variables

Prefer `const` over `let`. Use ternaries or early returns instead of reassignment.

```ts
// Good
const foo = condition ? 1 : 2

// Bad
let foo
if (condition) foo = 1
else foo = 2
```

### Control Flow

Avoid `else` statements. Prefer early returns.

```ts
// Good
function foo() {
  if (condition) return 1
  return 2
}

// Bad
function foo() {
  if (condition) return 1
  else return 2
}
```

### Complex Logic

When a function has several validation branches or supporting details, make the main function read as the happy path and move supporting details into small helpers below it.

```ts
// Good
export function loadThing(input: unknown) {
  const config = requireConfig(input)
  const metadata = readMetadata(input)
  return createThing({ config, metadata })
}

function requireConfig(input: unknown) {
  ...
}
```

- Keep helpers close to the code they support, below the main export when that improves readability.
- Do not over-abstract simple expressions into many single-use helpers; extract only when it names a real concept like `requireConfig` or `readMetadata`.
- Do not return `Effect` from helpers unless they actually perform effectful work. Synchronous parsing, validation, and option building should stay synchronous.
- Prefer Effect schema helpers such as `Schema.UnknownFromJsonString` and `Schema.decodeUnknownOption` over manual `JSON.parse` wrapped in `Effect.try` when parsing untrusted JSON strings.
- Add comments for non-obvious constraints and surprising behavior, not for obvious assignments or control flow.

### Schema Definitions (Drizzle)

Use snake_case for field names so column names don't need to be redefined as strings.

```ts
// Good
const table = sqliteTable("session", {
  id: text().primaryKey(),
  project_id: text().notNull(),
  created_at: integer().notNull(),
})

// Bad
const table = sqliteTable("session", {
  id: text("id").primaryKey(),
  projectID: text("project_id").notNull(),
  createdAt: integer("created_at").notNull(),
})
```

## Testing

- Avoid mocks as much as possible
- Test actual implementation, do not duplicate logic into tests
- Tests cannot run from repo root (guard: `do-not-run-tests-from-root`); run from package dirs like `packages/opencode`.

## Type Checking

- Always run `bun typecheck` from package directories (e.g., `packages/opencode`), never `tsc` directly.


## Notes (问题存档与沟通记录)

`notes/` 文件夹用于存档开发过程中发现的问题、讨论结论和沟通记录。每份笔记以 `NNN-简短标题.md` 命名，包含现象、日志证据、根因分析和建议修复方向。

当前存档：
- `001-subagent-ui-visibility.md` — 子 Agent 运行时 TUI 无状态指示，用户误以为 agent 停止工作

## OC — OpenCode 版本管理工具

`oc` 是仓库根目录下的 bash TUI 脚本，用于管理 opencode 二进制的版本升级。

- 升级渠道：`https://github.com/LeXwDeX/opencode/releases`
- 依赖：bash, curl, tar/unzip, fzf（必需）；gh（可选）
- 用法：`./oc` 启动 TUI 交互，支持版本选择、安装、升级
- 源码维护在本仓库根目录，修改后直接提交即可
